class Solution {
public:
    int titleToNumber(string s) {
        int count = 0;
        for(int i = 0;i < s.length();i++)
        {
            count = count * 26 + int(s[i])-64;   
        }
        return count;
    }
};
